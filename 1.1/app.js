let num = prompt("Введите число: ");

if (isNaN(num)) {
    console.log("Введеная информация не является числом")
} else {
    let numl = num.length;
    console.log (numl);
    console.log (num[0]);
}

